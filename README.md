## 自定义运行命令

## 工作区 .vscode 目录下 settings.json 配置

```json
"commandRunVscode.command": "npm run dev", // 默认运行命令
"commandRunVscode.commandList": ["npm run dev", "npm run dev2", "npm run dev3", "npm run dev4"] // 自定义运行命令
```

- 编辑器左上角运行图标, 运行默认命令
- 点击状态栏图标, 运行默认命令
- 在tooltip中, 选择自定义命令
